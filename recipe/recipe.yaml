# For more information about this format see: https://prefix-dev.github.io/rattler-build/latest/reference/recipe_file/
context:
  name: vte
  version: "0.84.1"
  version_majmin: ${{ (version | split('.'))[:2] | join('.') }}

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: "https://download.gnome.org/sources/${{ name }}/${{ version_majmin }}/${{ name }}-${{ version }}.tar.xz"
  sha256: aca1caa8478aebcdbb1d67897fb3511eb7601debae6810e16a15b6fa25f31ac8

build:
  number: 0
  script: |
    meson setup ${MESON_ARGS} \
        --prefix=$PREFIX \
        --default-library=shared \
        --wrap-mode=nofallback \
        -D_systemd=false \
        -Dgir=true \
        -Ddocs=false \
        -Dvapi=false \
        -Dgtk3=false \
        -Dfribidi=true \
        -Dicu=true \
        -Dglade=false \
        builddir
    ninja -v -C builddir -j ${CPU_COUNT}
    ninja -C builddir install -j ${CPU_COUNT}
  skip:
    - win
    - osx

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ stdlib("c") }}
    - gobject-introspection
    - meson
    - ninja
    - pkg-config
  host:
    - cairo
    - expat
    - fontconfig
    - freetype
    - fribidi
    - glib
    - gnutls
    - gobject-introspection
    - gdk-pixbuf
    - gtk4
    - icu
    - lz4-c
    - pango
    - pcre2
    - zlib
    # v0.79+
    - fast_float
    # v0.81+
    - simdutf
    - fmt

tests:
  - script: run_test.py
    requirements:
      run:
        - pygobject
  - package_contents:
      files:
        - if: unix
          then:
            - lib/girepository-1.0/Vte-3.91.typelib
about:
  homepage: https://gitlab.gnome.org/GNOME/vte
  summary: "A GNOME Virtual Terminal library (Vte)"
  description: |
    VTE provides a virtual terminal widget for GTK applications.
  license: LGPL-3.0-or-later
  license_file: [COPYING.CC-BY-4-0, COPYING.GPL3, COPYING.LGPL3, COPYING.XTERM]
  documentation: https://gnome.pages.gitlab.gnome.org/vte/
  repository: https://gitlab.gnome.org/GNOME/vte

extra:
  recipe-maintainers:
    - araffin
